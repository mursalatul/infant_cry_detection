# rotating card template
<style>
    .rotating-card-section {
        padding: 4rem 0;
        background: #FBFBFB;
        color: white;
    }
    
    .rotating-full-card {
      position: relative;
      width: 200px;
      height: 300px;
      perspective: 1000px;
      margin: 0 auto;
    }
    
    .rotating-full-card:hover .rotating-full-card-inner {
      transform: rotateY(180deg);
    }
.rotating-full-card-inner {
  position: relative;
  width: 100%;
  height: 100%;
  text-align: center;
  transform-style: preserve-3d;
  transition: transform 0.6s;
}


.rotating-card-front,
.rotating-card-back {
  position: absolute;
  width: 100%;
  height: 100%;
  backface-visibility: hidden;
  border-radius: 15px;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
}
 
.rotating-card-front {
  background: linear-gradient(to bottom right, var(--primary-color), #E8F9FF);
  color: black;
}

.rotating-card-front img {
  width: 100px;
  height: 100px;
  border-radius: 50%;
  margin-bottom: 1rem;
}

.rotating-card-back {
  background: #333;
  color: white;
  transform: rotateY(180deg);
  padding: 1rem;
  text-align: center;
}
</style>
<section class='rotating-card-section'>
    <div class="container">
        <div class="rotating-full-card">
          <div class="rotating-full-card-inner">
            <div class="rotating-card-front">
              <!-- card front content start here -->
              <!-- card front content end here -->
            </div>
            <div class="rotating-card-back">
                <!-- card back content start here -->
                <!-- card back content end here -->
            </div>
          </div>
        </div>
    </div>
</section>

